# fmt: off
# GENERATED FILE -- do not edit by hand.
# Regenerated from areas/*.are by tools/are_to_primesud.py
# (run via tools/regen_areas.py); fix the .are or the converter.
# Area: Player Estates
# Source: QuickMUD/ROM 2.4
# VNUM ranges: 17700-17899
# Credits: {All } 1stMud Player Estates


AREA = {
    "name":     'Player Estates',
    "builders": 'None',
    "vnums":    (17700, 17899),
    "credits":  '{All } 1stMud Player Estates',
    "lvl_comment": 'All',
    "levels":   (1, 50),
}

# -- Mob templates --------------------------------------------------------------
# hp_dice / mana_dice / damage: (num_dice, die_size, bonus)
# armor: (pierce, bash, slash, exotic), raw .are units
# hitroll: from mob level line
MOBILES = {
}

# -- Rooms ----------------------------------------------------------------------
ROOMS = {
    17700: {
        "name": 'Entrance to the Player Estates',
        "desc": 'A quiet lane leads from Midgaard Park to the Player Estates office.',
        "exits": {
            "e": 17701,
            "w": 3109,
        },
        "sector": 'inside',
    },
    17701: {
        "name": 'The Player Estates Office',
        "desc": 'A polished oak door stands ready for an adventurer seeking a place of their own.',
        "exits": {
            "n": {"to": 17702, "desc": 'You see the door to an unclaimed home.', "keyword": 'door', "isdoor": True, "closed": True, "locked": True, "pickproof": True, "nopass": True, "key": 17700},
            "w": 17700,
        },
        "sector": 'inside',
    },
    17702: {
        "name": 'An Unclaimed Home',
        "desc": 'A quiet, unfurnished room waits for someone to make it their own.',
        "exits": {
            "s": {"to": 17701, "desc": 'You see the door to the estate office.', "keyword": 'door', "isdoor": True, "closed": True, "pickproof": True, "nopass": True, "key": 17700},
        },
        "flags": {"indoors": True, "save_objs": True},
        "sector": 'inside',
    },
}

# -- Item templates -------------------------------------------------------------
OBJECTS = {
    17700: {
        "keywords":    'key estate home',
        "short_descr": 'an estate key',
        "description": 'An estate key lies here.',
        "material":    'unknown',
        "type": 'key',
        "wear_flags": {"take": True, "hold": True},
        "condition": 0,
        "level": 0, "weight": 0, "value": 0,
    },
}

# -- Resets ---------------------------------------------------------------------
# ("M", mob_vnum, global_limit, room_vnum, room_limit) -- spawn mob up to limits
# ("O", item_vnum, room_vnum)                          -- place one item copy in room
# ("E", item_vnum, slot_name, limit)                   -- equip item on last M mob
# ("G", item_vnum, limit)                              -- give item to last M mob inventory
# E/G/P limit: raw ROM reset-count field (cf. db.c reset_room): a value
# > 50 is a legacy encoding meaning limit 6; -1 means unlimited, and 0
# also means unlimited for E/G specifically (not P). Enforced at runtime.
# ("P", item_vnum, limit, container_vnum, max)         -- fill container in room up to max (capped by limit)
# ("R", room_vnum, num_dirs)                           -- shuffle first num_dirs exits (skipped on doors)
# D resets are baked into room exit flags at conversion time
RESETS = (
)

# -- Helps ---------------------------------------------------------------------
HELPS = (
)

# -- Socials -------------------------------------------------------------------
SOCIALS = (
)

# -- MobProgs ------------------------------------------------------------------
# (vnum, code) -- mob program code blocks, referenced by mob triggers
MOBPROGS = {
}

# -- ObjProgs ------------------------------------------------------------------
# (vnum, code) -- object program code blocks, referenced by object triggers
OBJPROGS = {
}

# -- RoomProgs -----------------------------------------------------------------
# (vnum, code) -- room program code blocks, referenced by room triggers
ROOMPROGS = {
}
